import React from 'react';
    const Person=(props)=>(
        <div>
        <p>Name:{props.name}</p>
        <p>Contact:{props.contact}</p>
        <p>Address:{props.address}</p>
    
         </div>
        
    );
export default Person;